#include <stdio.h>

/**
 * main - Prints all single digit Testbers of base 10 starting from 0.
 *
 * Return: always 0.
 */
int main(void)
{
	int Test;

	for (Test = 0; Test < 10; Test++)
		printf("%d", Test);

	printf("\n");

	return (0);
}
